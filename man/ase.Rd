% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_embedders.R
\name{ase}
\alias{ase}
\title{Compute an adjacency spectral embedding}
\usage{
ase(
  A,
  d,
  directed = FALSE,
  reconstruct = FALSE,
  align_with = NULL,
  ram_check = FALSE
)
}
\arguments{
\item{A}{A finite square dense or sparse adjacency matrix.}

\item{d}{Positive embedding dimension.}

\item{directed}{Whether to treat \code{A} as directed.}

\item{reconstruct}{Whether to return the rank-\code{d} matrix reconstruction.}

\item{align_with}{Optional \code{nrow(A)}-by-\code{d} reference embedding used for
orthogonal Procrustes alignment.}

\item{ram_check}{Whether to report a conservative RAM preflight estimate.}
}
\value{
An embedding object containing coordinates and decomposition details.
}
\description{
Embeds a dense or sparse adjacency matrix in \code{d} dimensions.
}
\details{
For an undirected \code{A}, the embedding uses the \code{d} eigenvalues of
largest magnitude and forms \eqn{\hat Z = U |\Lambda|^{1/2}}. Signed
eigenvalues are retained so reconstruction uses
\eqn{U \Lambda U^T}. Directed input uses a rank-\code{d} SVD to produce left
and right embeddings. A supplied \code{align_with} rotates both embeddings
together, preserving their reconstructed matrix.
}
\examples{
A <- generate_rdpg(n = 200, d = 3, seed = 8, ncores = 1)
fit <- ase(A, d = 3)
dim(fit$Z_hat)
}
\seealso{
\code{\link[=spectral_cluster]{spectral_cluster()}}, \code{\link[=generate_rdpg]{generate_rdpg()}}, \code{\link[=netcrop_rdpg]{netcrop_rdpg()}}
}
