% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x6_other_algos.R
\name{dkest_tune_regularizer}
\alias{dkest_tune_regularizer}
\title{Tune a degree-regularized spectral model}
\usage{
dkest_tune_regularizer(
  A,
  K,
  tau_candidates,
  use_laplacian = TRUE,
  use_dcbm = TRUE,
  dcbm_est_method = c("plugin", "spectral"),
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  failure_handling = c("stop", "omit"),
  retain_intermediates = c("all", "minimal")
)
}
\arguments{
\item{A}{Finite symmetric, loop-free adjacency matrix.}

\item{K}{Fixed positive number of communities.}

\item{tau_candidates}{Unique finite nonnegative regularization candidates.}

\item{use_laplacian}{Use a normalized graph Laplacian.}

\item{use_dcbm}{Fit a degree-corrected rather than ordinary block model.}

\item{dcbm_est_method}{DCBM estimator, either \code{"plugin"} or \code{"spectral"}.}

\item{ncores}{Positive worker count.}

\item{seed}{Optional nonnegative reproducibility seed.}

\item{verbose}{Print progress messages.}

\item{force_windows}{Use the Windows-compatible parallel backend.}

\item{ram_check}{Report conservative RAM demand.}

\item{failure_handling}{Stop or omit failed candidates.}

\item{retain_intermediates}{Retain all or minimal candidate fits.}
}
\value{
A \code{netcrop_regularizer} object containing \code{tau_hat}, the selected DK
statistic, per-candidate numerator, denominator, and ratio, diagnostics,
seeds, worker counts, timing, RAM information, and optional raw fits.
}
\description{
Selects a spectral regularizer using the DKEST eigenspectral-ratio
criterion.
}
\details{
Each candidate regularizes the observed matrix, optionally constructs its
normalized Laplacian, clusters the fixed-\code{K} embedding, estimates SBM or
DCBM probabilities, and computes the ratio between selected residual and
fitted eigenvalues. Candidate failures are audited explicitly.
}
\examples{
\donttest{
A <- generate_sbm(n = 200, K = 3, alpha = 0.5, beta = 0.1,
                  seed = 35, ncores = 1)
dkest_tune_regularizer(A, K = 3, tau_candidates = c(0, 0.1),
                       ncores = 1, seed = 36, verbose = FALSE)
}
}
\seealso{
\code{\link[=netcrop_tune_regularizer]{netcrop_tune_regularizer()}}, \code{\link[=mult_reg_spectral_cluster]{mult_reg_spectral_cluster()}}
}
