% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x6_other_algos.R
\name{ecv_stability_blockmodel}
\alias{ecv_stability_blockmodel}
\title{Select block-model size with edge cross-validation}
\usage{
ecv_stability_blockmodel(
  A,
  max_K,
  train_proportion = 0.9,
  cv = 3L,
  nrep = 20L,
  tau = 0,
  losses = "sse",
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  failure_handling = c("stop", "omit"),
  retain_intermediates = c("all", "minimal")
)
}
\arguments{
\item{A}{Binary, symmetric, loop-free adjacency matrix.}

\item{max_K}{Maximum candidate community count; candidates are \code{1:max_K}.}

\item{train_proportion}{Proportion of dyads retained for training.}

\item{cv}{Positive number of edge-sampling folds.}

\item{nrep}{Positive number of repeated ECV runs.}

\item{tau}{Nonnegative block-model regularization value.}

\item{losses}{Canonical loss names to evaluate.}

\item{ncores}{Positive outer worker count; folds run sequentially.}

\item{seed}{Optional nonnegative reproducibility seed.}

\item{verbose}{Print progress messages.}

\item{force_windows}{Use the Windows-compatible parallel backend.}

\item{ram_check}{Report conservative RAM demand.}

\item{failure_handling}{Stop or omit failed repetitions.}

\item{retain_intermediates}{Retain all or minimal legacy results.}
}
\value{
A \code{netcrop_blockmodel} object with \code{algorithm = "ECV"}, tidy losses,
selections, candidate metadata, realized seeds, failures, worker counts,
timing, RAM diagnostics, and optional raw output.
}
\description{
Runs repeated edge-sampling cross-validation over every community count from
one through \code{max_K} using the preserved ECV block-model implementation.
}
\details{
The wrapper validates binary undirected loop-free input, candidate and
sampling feasibility, dependencies, seeds, workers, and losses. Repetitions
run through \code{\link[=uni_mclapply]{uni_mclapply()}} while folds within each repetition remain
sequential. Raw ECV results are audited and converted to the shared tidy
loss schema; failed repetitions either stop or are explicitly omitted.
}
\examples{
\donttest{
A <- generate_sbm(n = 200, K = 3, alpha = 0.5, beta = 0.1,
                  seed = 6, ncores = 1)
ecv_stability_blockmodel(A, max_K = 5, cv = 2, nrep = 1,
                         ncores = 1, seed = 7, verbose = FALSE)
}
}
\references{
Li, T., Levina, E., and Zhu, J. (2020). Network cross-validation by edge
sampling. \emph{Biometrika}, 107(2), 257-276. \doi{10.1093/biomet/asaa006}
}
\seealso{
\code{\link[=ecv_stability_rdpg]{ecv_stability_rdpg()}}, \code{\link[=ncv_stability_blockmodel]{ncv_stability_blockmodel()}},
\code{\link[=netcrop_blockmodel]{netcrop_blockmodel()}}
}
