% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x6_other_algos.R
\name{ecv_stability_rdpg}
\alias{ecv_stability_rdpg}
\title{Select RDPG dimension with edge cross-validation}
\usage{
ecv_stability_rdpg(
  A,
  max_d,
  cv = 3L,
  nrep = 1L,
  train_proportion = 0.9,
  losses = c("mse", "bin_dev", "auc_as_loss"),
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  failure_handling = c("stop", "omit"),
  retain_intermediates = c("all", "minimal")
)
}
\arguments{
\item{A}{Binary, symmetric, loop-free adjacency matrix.}

\item{max_d}{Maximum candidate dimension; candidates are \code{1:max_d}.}

\item{cv}{Positive number of edge-sampling folds.}

\item{nrep}{Positive number of repeated ECV runs.}

\item{train_proportion}{Proportion of dyads retained for training.}

\item{losses}{Canonical loss names to evaluate: \code{"mse"}, \code{"sse"},
\code{"bin_dev"}, and \code{"auc_as_loss"}.}

\item{ncores}{Positive outer worker count; folds run sequentially.}

\item{seed}{Optional nonnegative reproducibility seed.}

\item{verbose}{Print progress messages.}

\item{force_windows}{Use the Windows-compatible parallel backend.}

\item{ram_check}{Report conservative RAM demand.}

\item{failure_handling}{Stop or omit failed repetitions.}

\item{retain_intermediates}{Retain all or minimal legacy results.}
}
\value{
A \code{netcrop_rdpg} object with \code{algorithm = "ECV"}, tidy loss curves,
selections, holdout metadata, realized seeds, failures, worker counts,
timings, RAM diagnostics, and optional legacy output.
}
\description{
Runs repeated edge-sampling cross-validation over every symmetric-RDPG
dimension from one through \code{max_d}.
}
\details{
The preserved RDPG ECV reconstruction routines run inside an isolated
internal environment. The wrapper validates graph and holdout feasibility,
supplies namespace-safe dependencies, audits all results, converts them to
tidy losses, and retains independently realized repetition seeds.
}
\examples{
\donttest{
A <- as.matrix(generate_rdpg(n = 200, d = 3, average_degree = 8,
                             seed = 33, ncores = 1))
ecv_stability_rdpg(A, max_d = 5, cv = 2, nrep = 1,
                   ncores = 1, seed = 34, verbose = FALSE)
}
}
\references{
Li, T., Levina, E., and Zhu, J. (2020). Network cross-validation by edge
sampling. \emph{Biometrika}, 107(2), 257-276. \doi{10.1093/biomet/asaa006}
}
\seealso{
\code{\link[=ecv_stability_blockmodel]{ecv_stability_blockmodel()}}, \code{\link[=netcrop_rdpg]{netcrop_rdpg()}}
}
