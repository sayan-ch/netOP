% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_math_helpers.R
\name{eig_decomp}
\alias{eig_decomp}
\title{Compute an eigendecomposition}
\usage{
eig_decomp(
  A,
  d,
  only_values = FALSE,
  scale_by = c("none", "dimension", "sparsity"),
  use_laplacian = FALSE,
  engine = c("rspectra", "base", "irlba"),
  force_engine = FALSE,
  order_by = c("magnitude", "value"),
  safe_d_multiplier = 1,
  validate_inputs = TRUE
)
}
\arguments{
\item{A}{A finite symmetric square dense or sparse matrix.}

\item{d}{Positive number of eigencomponents to return.}

\item{only_values}{Return eigenvalues without eigenvectors.}

\item{scale_by}{Scale by \code{"none"}, matrix \code{"dimension"}, or entry
\code{"sparsity"}.}

\item{use_laplacian}{Transform \code{A} with \code{\link[=graph_laplacian]{graph_laplacian()}} first.}

\item{engine}{Decomposition backend.}

\item{force_engine}{Stop instead of falling back when the selected partial
backend is unavailable or fails.}

\item{order_by}{Order by signed \code{"value"} or absolute \code{"magnitude"}.}

\item{safe_d_multiplier}{Multiplier for extra partial components computed
before selecting the requested components.}

\item{validate_inputs}{Validate matrix and option inputs.}
}
\value{
A list with \code{values} and, unless \code{only_values}, \code{vectors}.
}
\description{
Computes selected eigenpairs of a finite symmetric matrix. Partial
decomposition falls back to \code{\link[base:eigen]{base::eigen()}} unless \code{force_engine = TRUE}.
The \code{"irlba"} option is accepted for compatibility but redirected because
an SVD is not a signed eigendecomposition.
}
\examples{
eig_decomp(diag(c(3, 2, 1)), d = 2, engine = "base")
}
\seealso{
\code{\link[=singular_decomp]{singular_decomp()}}, \code{\link[=ase]{ase()}}
}
