% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_estimators.R
\name{estimate_sbm}
\alias{estimate_sbm}
\alias{estimate_dcbm}
\title{Estimate stochastic block models}
\usage{
estimate_sbm(
  A,
  g,
  K = max(g),
  fold_nodes = NULL,
  directed = FALSE,
  self_loops = FALSE,
  validate_inputs = TRUE,
  stabilizer = 0.01
)

estimate_dcbm(
  A,
  g,
  K = max(g),
  method = c("plugin", "spectral"),
  fold_nodes = NULL,
  row_norm = NULL,
  psi_omit = 0L,
  stabilizer = 0.01,
  spectral_engine = c("rspectra", "irlba", "base"),
  spectral_options = list(),
  validate_inputs = TRUE
)
}
\arguments{
\item{A}{A finite full network matrix or supported rectangular NCV layout.}

\item{g}{Positive integer community labels for the full network.}

\item{K}{Positive number of communities.}

\item{fold_nodes}{Optional original indices of held-out NCV nodes.}

\item{directed}{Whether edges are directed. Used by \code{estimate_sbm()}.}

\item{self_loops}{Whether diagonal edges are included. Used by
\code{estimate_sbm()}.}

\item{validate_inputs}{Whether to validate inputs; only audited internal
callers should disable this.}

\item{stabilizer}{Nonnegative density-scaled pseudocount used by the SBM and
DCBM plug-in estimators.}

\item{method}{DCBM estimator, either \code{"plugin"} or \code{"spectral"}.}

\item{row_norm}{Optional supplied DCBM spectral row norms.}

\item{psi_omit}{Number of trailing nodes omitted from returned DCBM degree
parameters.}

\item{spectral_engine}{Spectral-decomposition backend.}

\item{spectral_options}{Named options passed to the spectral decomposition.}
}
\value{
A fitted block-model object.
}
\description{
Estimates SBM or DCBM parameters from dense, sparse, full, or supported NCV
adjacency layouts.
}
\details{
With \code{fold_nodes = NULL}, \code{A} is the full network. With fold nodes,
\code{A} may be the full matrix, non-fold rows by all columns, all rows by fold
columns, or non-fold rows by fold columns. The rectangular SBM estimator
counts represented dyads, pools both available block orientations for
undirected input, and identifies genuine self-pairs from original node
indices.

\code{estimate_dcbm()} uses either the historical stabilized
degree/block-sum plug-in estimator or spectral row norms. Full square
networks use \code{\link[=eig_decomp]{eig_decomp()}}; rectangular NCV layouts use
\code{\link[=singular_decomp]{singular_decomp()}}. \code{row_norm} may be a full-network vector, a vector for
the columns of \code{A} when all row nodes occur there, or a named list with
\code{row} and \code{col} components matching the dimensions of \code{A}.
}
\section{Functions}{
\itemize{
\item \code{estimate_dcbm()}: Estimate degree-corrected block and node
parameters.

}}
\examples{
A <- generate_sbm(n = 200, K = 3, alpha = 0.4, beta = 0.1,
                  seed = 11, ncores = 1)
g <- get_generator_parameters(A)$g_true
B_hat <- estimate_sbm(A, g, K = 3)
dim(B_hat)
}
\seealso{
\code{\link[=estimate_sbm_P_hat]{estimate_sbm_P_hat()}}, \code{\link[=estimate_dcbm_P_hat]{estimate_dcbm_P_hat()}}, \code{\link[=spectral_cluster]{spectral_cluster()}}
}
