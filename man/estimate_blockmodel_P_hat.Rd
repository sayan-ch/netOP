% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_estimators.R
\name{estimate_sbm_P_hat}
\alias{estimate_sbm_P_hat}
\alias{estimate_dcbm_P_hat}
\title{Reconstruct block-model probability matrices}
\usage{
estimate_sbm_P_hat(
  A,
  g,
  K = max(g),
  fold_nodes = NULL,
  directed = FALSE,
  self_loops = FALSE,
  lower_clip = 0,
  upper_clip = 1
)

estimate_dcbm_P_hat(
  A,
  g,
  K = max(g),
  method = c("plugin", "spectral"),
  fold_nodes = NULL,
  row_norm = NULL,
  psi_omit = 0L,
  stabilizer = 0.01,
  spectral_engine = c("rspectra", "irlba", "base"),
  spectral_options = list(),
  self_loops = FALSE,
  lower_clip = 0,
  upper_clip = 1
)
}
\arguments{
\item{A}{A finite full network matrix or supported rectangular NCV layout.}

\item{g}{Positive integer community labels for the full network.}

\item{K}{Positive number of communities.}

\item{fold_nodes}{Optional original indices of held-out NCV nodes.}

\item{directed}{Whether edges are directed. Used by \code{estimate_sbm_P_hat()}.}

\item{self_loops}{Whether diagonal probabilities are retained.}

\item{lower_clip, upper_clip}{Finite lower and upper bounds applied to fitted
probabilities.}

\item{method}{DCBM estimator, either \code{"plugin"} or \code{"spectral"}.}

\item{row_norm}{Optional supplied DCBM spectral row norms.}

\item{psi_omit}{Number of trailing nodes omitted from returned DCBM degree
parameters.}

\item{stabilizer}{Nonnegative plug-in denominator stabilizer.}

\item{spectral_engine}{Spectral-decomposition backend.}

\item{spectral_options}{Named options passed to the spectral decomposition.}
}
\value{
A fitted edge-probability matrix.
}
\description{
Reconstructs fitted SBM or DCBM edge probabilities.
}
\details{
In NCV mode, \code{estimate_sbm_P_hat()} returns the fold-by-fold
probability matrix implied by the block estimate; otherwise it returns
probabilities for all nodes.

\code{estimate_dcbm_P_hat()} computes
\eqn{\hat P_{ij} = \hat\psi_i \hat\psi_j \hat B_{g_i,g_j}}. In NCV mode,
degree parameters are estimated for fold nodes and the result is
fold-by-fold. With \code{psi_omit}, the result covers retained trailing nodes.
}
\section{Functions}{
\itemize{
\item \code{estimate_dcbm_P_hat()}: Reconstruct degree-corrected block-model
probabilities.

}}
\examples{
A <- generate_sbm(n = 200, K = 3, alpha = 0.4, beta = 0.1,
                  seed = 12, ncores = 1)
g <- get_generator_parameters(A)$g_true
P_hat <- estimate_sbm_P_hat(A, g, K = 3)
dim(P_hat)
}
\seealso{
\code{\link[=estimate_sbm]{estimate_sbm()}}, \code{\link[=estimate_dcbm]{estimate_dcbm()}}, \code{\link[=clip_probabilities]{clip_probabilities()}}
}
