% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_network_generator.R
\name{generate_dcbm}
\alias{generate_dcbm}
\alias{generate_sbm}
\title{Generate block-model networks}
\usage{
generate_dcbm(
  n = NULL,
  K = NULL,
  g_true = NULL,
  community_probabilities = NULL,
  P_block = NULL,
  alpha = 0.2,
  beta = 0.2,
  psi = NULL,
  degree_distribution = generate_inverse_beta_degree_parameters,
  degree_args = NULL,
  degree_scale = c("none", "max_by_community", "mean_one_by_community"),
  sparsity_multiplier = 1,
  average_degree = NULL,
  average_degree_method = c("naive", "calibrated"),
  lower_clip = 0,
  upper_clip = 1,
  representation = c("sparse", "dense"),
  directed = FALSE,
  self_loops = FALSE,
  seed = NULL,
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE)
)

generate_sbm(
  n = NULL,
  K = NULL,
  g_true = NULL,
  community_probabilities = NULL,
  P_block = NULL,
  alpha = 0.2,
  beta = 0.2,
  sparsity_multiplier = 1,
  average_degree = NULL,
  average_degree_method = c("naive", "calibrated"),
  lower_clip = 0,
  upper_clip = 1,
  representation = c("sparse", "dense"),
  directed = FALSE,
  self_loops = FALSE,
  seed = NULL,
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE)
)
}
\arguments{
\item{n}{Positive number of nodes.}

\item{K}{Positive number of communities.}

\item{g_true}{Optional supplied community-label vector.}

\item{community_probabilities}{Optional length-\code{K} community probabilities.}

\item{P_block}{Optional \code{K}-by-\code{K} block-probability matrix.}

\item{alpha, beta}{Within- and between-community probabilities used when
\code{P_block} is omitted.}

\item{psi}{Optional supplied DCBM degree parameters.}

\item{degree_distribution}{Function used to generate DCBM degree parameters.}

\item{degree_args}{Optional named list passed to \code{degree_distribution}.}

\item{degree_scale}{Community-wise degree-normalization method.}

\item{sparsity_multiplier}{Nonnegative multiplier applied to probabilities.}

\item{average_degree}{Optional target expected average degree.}

\item{average_degree_method}{Scaling method, either \code{"naive"} or
\code{"calibrated"}.}

\item{lower_clip, upper_clip}{Finite probability clipping bounds.}

\item{representation}{Whether to return a sparse or dense adjacency matrix.}

\item{directed}{Whether to generate a directed network.}

\item{self_loops}{Whether diagonal edges may be generated.}

\item{seed}{Optional nonnegative reproducibility seed.}

\item{ncores}{Positive worker count.}
}
\value{
A generated adjacency matrix; see \code{\link[=get_generator_parameters]{get_generator_parameters()}}.
}
\description{
\code{generate_dcbm()} generates a degree-corrected stochastic block model;
\code{generate_sbm()} is its ordinary-block-model twin with unit degree effects.
Both return dense or sparse adjacency matrices.

\code{generate_sbm()} fixes every degree parameter to one.
}
\examples{
A <- generate_sbm(n = 200, K = 3, alpha = 0.4, beta = 0.1,
                  seed = 3, ncores = 1)
get_generator_parameters(A)
}
\seealso{
\code{\link[=generate_community_labels]{generate_community_labels()}}, \code{\link[=estimate_sbm]{estimate_sbm()}}, \code{\link[=estimate_dcbm]{estimate_dcbm()}}
}
