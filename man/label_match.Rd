% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_estimators.R
\name{label_match_greedy}
\alias{label_match_greedy}
\alias{label_match_brute_force}
\title{Match community labels}
\usage{
label_match_greedy(
  match_this,
  standard,
  K = max(c(match_this, standard)),
  algorithm = c("greedy", "hungarian"),
  return_mapping = FALSE
)

label_match_brute_force(
  match_this,
  standard,
  K = max(c(match_this, standard)),
  return_mapping = FALSE,
  confirm_large = NULL
)
}
\arguments{
\item{match_this}{Positive integer labels to relabel.}

\item{standard}{Positive integer target labels of the same length.}

\item{K}{Positive number of label classes.}

\item{algorithm}{Assignment method for \code{label_match_greedy()}: historical
greedy matching or globally optimal Hungarian matching.}

\item{return_mapping}{Whether to return mapping diagnostics with the labels.}

\item{confirm_large}{For exact matching with more than eight classes,
explicitly confirm factorial computation in noninteractive use.}
}
\value{
A relabeled vector, optionally with mapping diagnostics.
}
\description{
Aligns labels using greedy assignment or exact brute-force matching.
}
\details{
The greedy function uses exact shortcuts for identical and
two-community inputs, followed by historical maximum-overlap assignment.
Its \code{"hungarian"} option uses a dependency-free cubic-time assignment and
maximizes total agreement globally.

\code{label_match_brute_force()} generates permutations recursively and
scores them one at a time. Runtime remains factorial. For \code{K > 8}, an
interactive call requires confirmation and noninteractive callers must set
\code{confirm_large = TRUE}.
}
\section{Functions}{
\itemize{
\item \code{label_match_brute_force()}: Match labels by enumerating every permutation.

}}
\examples{
labels <- c(2, 2, 3, 3, 1, 1)
standard <- c(1, 1, 2, 2, 3, 3)
label_match_greedy(labels, standard, K = 3)$matched_labels
}
\seealso{
\code{\link[=spectral_cluster]{spectral_cluster()}}, \code{\link[=sonnet]{sonnet()}}
}
