% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x6_other_algos.R
\name{mult_reg_sonnet}
\alias{mult_reg_sonnet}
\title{Fit SONNET over multiple regularizers}
\usage{
mult_reg_sonnet(
  A,
  K,
  tau_candidates,
  num_subnetworks = NULL,
  overlap_size = NULL,
  extra_nrep = 0L,
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  matching_method = c("greedy", "hungarian", "brute_force"),
  confirm_large = TRUE,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  share_overlap = FALSE,
  parameter_select_options = list(),
  failure_handling = c("stop", "omit"),
  retain_fits = TRUE,
  ...
)
}
\arguments{
\item{A}{One adjacency matrix or a nonempty list of equal-sized matrices.}

\item{K}{Fixed positive number of communities.}

\item{tau_candidates}{Unique finite nonnegative regularization candidates.}

\item{num_subnetworks}{Number of overlapping subnetworks; \code{NULL} selects it
automatically.}

\item{overlap_size}{Number of overlap nodes; \code{NULL} selects it automatically.}

\item{extra_nrep}{Number of additional SONNET repetitions.}

\item{ncores}{Positive SONNET worker count.}

\item{seed}{Optional nonnegative reproducibility seed.}

\item{matching_method}{Label-alignment method.}

\item{confirm_large}{Confirm potentially factorial brute-force matching.}

\item{verbose}{Print progress messages.}

\item{force_windows}{Use the Windows-compatible parallel backend.}

\item{ram_check}{Report conservative RAM demand.}

\item{share_overlap}{Reuse one overlap across repetitions.}

\item{parameter_select_options}{Named automatic-partition options.}

\item{failure_handling}{Stop or omit failed candidate fits.}

\item{retain_fits}{Retain fitted \code{sonnet} objects.}

\item{...}{Named spectral-clustering arguments forwarded to \code{\link[=sonnet]{sonnet()}}.}
}
\value{
A \code{mult_reg_clustering} object containing SONNET labels and optional
fits by network and regularizer, parameters, tasks, failures, seeds, and
timing.
}
\description{
Fits SONNET across a requested regularization grid for one network or a
matched list.
}
\details{
Candidate fits run sequentially so \code{ncores} remains available to SONNET's
internal subnetwork computations. The same realized seed is reused across
regularization candidates within each network.
}
\examples{
\donttest{
A <- generate_sbm(n = 200, K = 3, alpha = 0.5, beta = 0.1,
                  seed = 39, ncores = 1)
mult_reg_sonnet(
  A, K = 3, tau_candidates = c(0, 0.1),
  num_subnetworks = 2, overlap_size = 20, ncores = 1,
  seed = 40, verbose = FALSE, spectral_engine = "base"
)
}
}
\seealso{
\code{\link[=sonnet]{sonnet()}}, \code{\link[=mult_reg_spectral_cluster]{mult_reg_spectral_cluster()}}
}
