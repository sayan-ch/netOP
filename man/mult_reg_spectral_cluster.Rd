% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x6_other_algos.R
\name{mult_reg_spectral_cluster}
\alias{mult_reg_spectral_cluster}
\title{Fit spectral clusterings over multiple regularizers}
\usage{
mult_reg_spectral_cluster(
  A,
  K,
  tau_candidates,
  laplacian = FALSE,
  normalize_laplacian = TRUE,
  handle_zero_degree_nodes = c("none", "random_label", "remove"),
  row_normalize = FALSE,
  spectral_method = c("eigen", "svd"),
  spectral_engine = c("RSpectra", "irlba", "base"),
  spectral_options = list(),
  cluster_engine = c("clara", "kmeans", "pam"),
  cluster_options = list(),
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  failure_handling = c("stop", "omit"),
  retain_fits = TRUE
)
}
\arguments{
\item{A}{One adjacency matrix or a nonempty list of equal-sized matrices.}

\item{K}{Fixed positive number of communities.}

\item{tau_candidates}{Unique finite nonnegative regularization candidates.}

\item{laplacian}{Convert adjacency matrices to graph Laplacians.}

\item{normalize_laplacian}{Use symmetric normalized Laplacians.}

\item{handle_zero_degree_nodes}{Zero-degree node policy.}

\item{row_normalize}{Normalize embedding rows before clustering.}

\item{spectral_method}{Use an eigen- or singular-vector representation.}

\item{spectral_engine}{Decomposition backend.}

\item{spectral_options}{Named list of decomposition options passed to the
\code{spectral_options} argument of \code{\link[=spectral_cluster]{spectral_cluster()}}.}

\item{cluster_engine}{Clustering backend.}

\item{cluster_options}{Named list of clustering options passed to the
\code{cluster_options} argument of \code{\link[=spectral_cluster]{spectral_cluster()}}.}

\item{ncores}{Positive task-worker count.}

\item{seed}{Optional nonnegative reproducibility seed.}

\item{verbose}{Print progress messages.}

\item{force_windows}{Use the Windows-compatible parallel backend.}

\item{ram_check}{Report conservative RAM demand.}

\item{failure_handling}{Stop or omit failed candidate fits.}

\item{retain_fits}{Retain fitted \code{spectral_cluster} objects.}
}
\value{
A \code{mult_reg_clustering} object containing labels and optional fits by
network and candidate, resolved parameters, task metadata, failures,
seeds, workers, timing, and RAM diagnostics.
}
\description{
Fits spectral clustering across a requested regularization grid for one
network or a matched list of networks.
}
\details{
Network-by-candidate tasks are parallelized. The same realized seed is used
across candidates within a network so stochastic clustering comparisons are
fair. Results can retain complete fitted objects or labels only.
}
\examples{
A <- generate_sbm(n = 200, K = 3, alpha = 0.5, beta = 0.1,
                  seed = 37, ncores = 1)
fits <- mult_reg_spectral_cluster(
  A, K = 3, tau_candidates = c(0, 0.1), ncores = 1,
  seed = 38, verbose = FALSE, spectral_engine = "base",
  cluster_engine = "kmeans"
)
}
\seealso{
\code{\link[=dkest_tune_regularizer]{dkest_tune_regularizer()}}, \code{\link[=netcrop_tune_regularizer]{netcrop_tune_regularizer()}}
}
