% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-docs.R, R/x6_other_algos.R
\name{ncv_stability}
\alias{ncv_stability}
\alias{ncv_stability_blockmodel}
\title{Node cross-validation stability selection}
\usage{
ncv_stability_blockmodel(
  A,
  max_K,
  cv = 3L,
  nrep = 20L,
  dc_est = c("spectral", "plugin"),
  tau = 0,
  use_laplacian = FALSE,
  losses = c("sse", "bin_dev", "auc_as_loss"),
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  failure_handling = c("stop", "omit"),
  retain_intermediates = c("all", "minimal")
)
}
\arguments{
\item{A}{A binary, symmetric, loop-free adjacency matrix.}

\item{max_K}{Maximum community count; candidates are \code{1:max_K}.}

\item{cv, nrep}{Numbers of folds and repetitions.}

\item{dc_est}{DCBM estimator (\code{"spectral"} or \code{"plugin"}).}

\item{tau, use_laplacian}{Spectral regularization controls.}

\item{losses}{Canonical loss names.}

\item{ncores}{Positive outer worker count.}

\item{seed}{Optional reproducibility seed.}

\item{verbose, force_windows, ram_check}{Diagnostic and backend controls.}

\item{failure_handling}{Stop or omit failed repetitions.}

\item{retain_intermediates}{Retain all or minimal repetition output.}
}
\value{
A \code{netcrop_blockmodel} result with \code{algorithm = "NCV"}, tidy
losses, selections, fold metadata, seeds, and diagnostics.
}
\description{
Repeated node cross-validation for selecting the number of block-model
communities. This is the netOP authors' implementation of the exact
algorithm described in Chen and Lei (2018). Numerical-stability measures
and failsafes were added without altering the algorithm in the paper.
Probabilities are clipped before logarithmic losses, zero-degree cases are
checked before normalization, non-finite candidate results are audited, and
failed repetitions can either stop or be omitted explicitly.
}
\examples{
A <- generate_sbm(n = 24, K = 2, alpha = 0.55, beta = 0.08,
                  representation = "dense", seed = 24, ncores = 1)
ncv_stability_blockmodel(A, max_K = 2, cv = 2, nrep = 1,
                         ncores = 1, seed = 26, verbose = FALSE,
                         losses = "sse")
}
\references{
Chen, K. and Lei, J. (2018). Network Cross-Validation for Determining the
Number of Communities in Network Data. \emph{Journal of the American Statistical
Association}, 113(521), 241-251. \doi{10.1080/01621459.2016.1246365}
}
