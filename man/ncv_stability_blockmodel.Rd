% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x6_other_algos.R
\name{ncv_stability_blockmodel}
\alias{ncv_stability_blockmodel}
\title{Select block-model size with node cross-validation}
\usage{
ncv_stability_blockmodel(
  A,
  max_K,
  cv = 3L,
  nrep = 20L,
  dc_est = c("spectral", "plugin"),
  tau = 0,
  use_laplacian = FALSE,
  losses = "sse",
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  failure_handling = c("stop", "omit"),
  retain_intermediates = c("all", "minimal")
)
}
\arguments{
\item{A}{Binary, symmetric, loop-free adjacency matrix.}

\item{max_K}{Maximum community count; candidates are \code{1:max_K}.}

\item{cv, nrep}{Positive numbers of folds and repetitions.}

\item{dc_est}{DCBM estimator, either \code{"spectral"} or \code{"plugin"}.}

\item{tau}{Nonnegative spectral regularization value.}

\item{use_laplacian}{Use Laplacian spectral representations.}

\item{losses}{Canonical loss names to evaluate.}

\item{ncores}{Positive outer worker count.}

\item{seed}{Optional nonnegative reproducibility seed.}

\item{verbose}{Print progress messages.}

\item{force_windows}{Use the Windows-compatible parallel backend.}

\item{ram_check}{Report conservative RAM demand.}

\item{failure_handling}{Stop or omit failed repetitions.}

\item{retain_intermediates}{Retain all or minimal repetition output.}
}
\value{
A \code{netcrop_blockmodel} object with \code{algorithm = "NCV"}, tidy fold
and repetition losses, selections, fold metadata, realized seeds,
failures, worker counts, timing, and optional raw output.
}
\description{
Repeats node cross-validation over every community count from one through
\code{max_K} for both SBM and DCBM candidates.
}
\details{
Each repetition partitions nodes into folds, fits candidates on rectangular
training blocks, and evaluates held-out dyads. Repetitions are parallelized,
fold stages remain sequential, and probabilities are clipped consistently
for logarithmic losses. Failed repetitions can stop or be omitted.
}
\examples{
\donttest{
A <- generate_sbm(n = 200, K = 3, alpha = 0.55, beta = 0.08,
                  seed = 24, ncores = 1)
ncv_stability_blockmodel(A, max_K = 5, cv = 2, nrep = 1,
                         ncores = 1, seed = 26, verbose = FALSE)
}
}
\references{
Chen, K. and Lei, J. (2018). Network Cross-Validation for Determining the
Number of Communities in Network Data. \emph{Journal of the American Statistical
Association}, 113(521), 241-251. \doi{10.1080/01621459.2016.1246365}
}
\seealso{
\code{\link[=ecv_stability_blockmodel]{ecv_stability_blockmodel()}}, \code{\link[=netcrop_blockmodel]{netcrop_blockmodel()}}
}
