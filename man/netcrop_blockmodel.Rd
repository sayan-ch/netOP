% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x2_netcrop_bm.R
\name{netcrop_blockmodel}
\alias{netcrop_blockmodel}
\alias{print.netcrop_blockmodel}
\alias{summary.netcrop_blockmodel}
\alias{print.summary.netcrop_blockmodel}
\alias{plot.netcrop_blockmodel}
\title{Select a block model with NETCROP}
\usage{
netcrop_blockmodel(
  A,
  K_candidates,
  num_subnetworks = NULL,
  overlap_size = NULL,
  nrep = 1L,
  losses = "sse",
  model_candidates = c("SBM", "DCBM"),
  sbm_est_options = list(),
  dcbm_est_options = list(),
  matching_method = c("greedy", "hungarian", "brute_force"),
  confirm_large = NULL,
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  parameter_select_options = list(),
  retain_intermediates = c("all", "minimal"),
  laplacian = FALSE,
  regularize_tau = 0
)

\method{print}{netcrop_blockmodel}(x, ...)

\method{summary}{netcrop_blockmodel}(object, ...)

\method{print}{summary.netcrop_blockmodel}(x, ...)

\method{plot}{netcrop_blockmodel}(x, aggregate = TRUE, ...)
}
\arguments{
\item{A}{Finite square symmetric binary adjacency matrix with zero diagonal,
including a supported sparse \code{Matrix} object.}

\item{K_candidates}{Non-empty vector of positive integer community counts;
duplicates are discarded. Conventionally this is \code{1:5}.}

\item{num_subnetworks}{Optional integer number of subnetworks, at least 2. If
it or \code{overlap_size} is \code{NULL}, missing values are selected with
\code{\link[=netcrop_param_select]{netcrop_param_select()}}.}

\item{overlap_size}{Optional positive integer overlap size. It must leave
enough non-overlap nodes for the requested subnetworks and candidates.}

\item{nrep}{Positive integer number of independent NETCROP repetitions.}

\item{losses}{Character vector naming prediction-loss functions available in
the calling environment, such as \code{"sse"}.}

\item{model_candidates}{Character vector selecting \code{"SBM"}, \code{"DCBM"}, or
both candidate model families.}

\item{sbm_est_options, dcbm_est_options}{Named lists of additional arguments
for the corresponding block-model estimators.}

\item{matching_method}{Label-alignment method: \code{"greedy"}, \code{"hungarian"}, or
\code{"brute_force"}.}

\item{confirm_large}{Optional logical passed to label matching to confirm an
unusually large brute-force matching problem.}

\item{ncores}{Positive integer worker count used across decomposition,
estimation, alignment, and loss tasks.}

\item{seed}{Optional nonnegative integer-like reproducibility seed.}

\item{verbose}{Whether to print progress messages.}

\item{force_windows}{Whether to force the Windows-compatible parallel
backend, including for backend testing on other platforms.}

\item{ram_check}{Whether to run RAM preflight checks before major operations.}

\item{parameter_select_options}{Named list of additional options passed to
\code{\link[=netcrop_param_select]{netcrop_param_select()}} when partition parameters are selected
automatically.}

\item{retain_intermediates}{Either \code{"all"} or \code{"minimal"}, controlling how
many fitted models and intermediate arrays are retained.}

\item{laplacian}{Whether to convert subnetwork adjacency matrices to graph
Laplacians before spectral clustering.}

\item{regularize_tau}{Nonnegative Laplacian regularization strength shared by
SBM and DCBM candidates.}

\item{x}{A \code{netcrop_blockmodel} or \code{summary.netcrop_blockmodel} object. For
plotting, it is the first result to display or compare.}

\item{...}{Additional plotting arguments or further \code{netcrop_blockmodel}
objects for comparison; ignored by print and summary methods.}

\item{object}{A fitted \code{netcrop_blockmodel} object to summarize.}

\item{aggregate}{Whether a plot should aggregate loss curves across
repetitions. A \code{netcrop_blockmodel} object in this position instead starts
a comparison plot.}
}
\value{
\code{netcrop_blockmodel()} returns an object of class
\code{netcrop_blockmodel} containing candidate-wise cross-validation losses,
repetition and overall selections, split and alignment diagnostics,
retained intermediates, resource information, and timing.
\code{summary.netcrop_blockmodel()} returns a compact summary object; print
methods return their input invisibly, and the plot method returns a
\code{ggplot} object.
}
\description{
Selects the number of communities and either the stochastic block model
(SBM), degree-corrected block model (DCBM), or both by overlapping-subnetwork
cross-validation. The procedure obtains spectral labels on each subnetwork,
estimates block-model parameters, aligns labels through the common overlap,
and evaluates predictions between every pair of non-overlap pieces.
}
\examples{
\donttest{
A <- generate_sbm(n = 200, K = 3, alpha = 0.5, beta = 0.1,
                  seed = 5, ncores = 1)
fit <- netcrop_blockmodel(
  A, K_candidates = 1:5, num_subnetworks = 2, overlap_size = 30,
  model_candidates = "SBM", ncores = 1, seed = 6, verbose = FALSE
)
fit
summary(fit)
}
}
\seealso{
\code{\link[=ecv_stability_blockmodel]{ecv_stability_blockmodel()}}, \code{\link[=ncv_stability_blockmodel]{ncv_stability_blockmodel()}}
}
