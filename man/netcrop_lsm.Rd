% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x4_netcrop_lsm.R
\name{netcrop_lsm}
\alias{netcrop_lsm}
\alias{print.netcrop_lsm}
\alias{summary.netcrop_lsm}
\alias{print.summary.netcrop_lsm}
\alias{plot.netcrop_lsm}
\title{Select latent-space dimension with NETCROP}
\usage{
netcrop_lsm(
  A,
  d_candidates,
  num_subnetworks = NULL,
  overlap_size = NULL,
  nrep = 1L,
  losses = "sse",
  lsm_options = list(),
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  parameter_select_options = list(),
  retain_intermediates = c("all", "minimal")
)

\method{print}{netcrop_lsm}(x, ...)

\method{summary}{netcrop_lsm}(object, ...)

\method{print}{summary.netcrop_lsm}(x, ...)

\method{plot}{netcrop_lsm}(x, aggregate = TRUE, ...)
}
\arguments{
\item{A}{Finite square symmetric binary adjacency matrix with zero diagonal,
including a supported sparse \code{Matrix} object.}

\item{d_candidates}{Non-empty vector of positive integer latent dimensions;
duplicates are discarded. Dimensions must be smaller than the effective
subgraph size; conventionally the candidate vector is \code{1:5}.}

\item{num_subnetworks}{Optional integer number of subnetworks, at least 2. If
it or \code{overlap_size} is \code{NULL}, missing values are selected with
\code{\link[=netcrop_param_select]{netcrop_param_select()}}.}

\item{overlap_size}{Optional positive integer overlap size. It must leave
enough non-overlap nodes for the requested subnetworks and dimensions.}

\item{nrep}{Positive integer number of independent NETCROP repetitions.}

\item{losses}{Character vector naming prediction-loss functions available in
the calling environment, such as \code{"sse"}.}

\item{lsm_options}{Named list of additional options passed to \code{\link[=lsm_pgd]{lsm_pgd()}}.}

\item{ncores}{Positive integer worker count used across fitting, alignment,
and loss tasks.}

\item{seed}{Optional nonnegative integer-like reproducibility seed.}

\item{verbose}{Whether to print progress messages.}

\item{force_windows}{Whether to force the Windows-compatible parallel
backend, including for backend testing on other platforms.}

\item{ram_check}{Whether to run RAM preflight checks before major operations.}

\item{parameter_select_options}{Named list of additional options passed to
\code{\link[=netcrop_param_select]{netcrop_param_select()}} when partition parameters are selected
automatically.}

\item{retain_intermediates}{Either \code{"all"} or \code{"minimal"}, controlling how
many fitted models and intermediate arrays are retained.}

\item{x}{A \code{netcrop_lsm} or \code{summary.netcrop_lsm} object to print or plot.}

\item{...}{Additional arguments, currently ignored by the print, summary,
and plot methods.}

\item{object}{A fitted \code{netcrop_lsm} object to summarize.}

\item{aggregate}{Whether a plot should aggregate loss curves across
repetitions.}
}
\value{
\code{netcrop_lsm()} returns an object of class \code{netcrop_lsm} containing
candidate-wise cross-validation losses, repetition and overall dimension
selections, split diagnostics, fitted LSM options, retained intermediates,
resource information, and timing. \code{summary.netcrop_lsm()} returns a compact
summary object; print methods return their input invisibly, and the plot
method returns a \code{ggplot} object.
}
\description{
Selects a latent-space-model (LSM) dimension by overlapping-subnetwork
cross-validation. The procedure fits every candidate on every subnetwork,
exactly reparameterizes each fitted inner-product model as a squared-distance
model, rigidly aligns fits through the overlap, and evaluates every unordered
pair of non-overlap pieces.
}
\examples{
\donttest{
A <- generate_lsm(n = 200, d = 3, K = 3, seed = 9, ncores = 1)
fit <- netcrop_lsm(
  A, d_candidates = 1:5, num_subnetworks = 2, overlap_size = 30,
  ncores = 1, seed = 10, verbose = FALSE,
  lsm_options = list(niter = 20)
)
fit
summary(fit)
}
}
\seealso{
\code{\link[=generate_lsm]{generate_lsm()}}, \code{\link[=lsm_pgd]{lsm_pgd()}}
}
