% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x0_helpers.R
\name{netcrop_param_select}
\alias{netcrop_param_select}
\title{Select NETCROP partition parameters}
\usage{
netcrop_param_select(test_prop = 0.02, n = NULL, o_range = c(0, 0.8))
}
\arguments{
\item{test_prop}{One finite number in \verb{(0, 0.5]} giving the target fraction
of unordered node pairs held out by a NETCROP split.}

\item{n}{Optional integer network size from 3 through R's maximum supported
integer. When supplied, integer subnetwork and overlap sizes are returned.}

\item{o_range}{Numeric vector with values in \verb{[0, 1]} locating candidate
overlaps between the lower and upper proportions implied by \code{test_prop}.}
}
\value{
A named list describing candidate subnetwork counts and overlap
proportions. When \code{n} is supplied, it also contains integer overlap and
piece sizes, remainder adjustments, and realized test proportions.
}
\description{
Converts a requested test proportion and one or more overlap-range positions
into NETCROP subnetwork counts and overlap proportions. An \code{o_range} value of
exactly one is replaced by \code{0.8} to avoid the singular upper endpoint. When
\code{n} is supplied, remainder nodes augment the overlap so the remaining nodes
divide evenly among the selected subnetworks.
}
\examples{
netcrop_param_select(test_prop = 0.05, n = 200, o_range = c(0, 0.8))
}
\seealso{
\code{\link[=netcrop_blockmodel]{netcrop_blockmodel()}}, \code{\link[=netcrop_rdpg]{netcrop_rdpg()}}, \code{\link[=netcrop_lsm]{netcrop_lsm()}}
}
