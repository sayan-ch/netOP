% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x3_netcrop_rdpg.R
\name{netcrop_rdpg}
\alias{netcrop_rdpg}
\alias{print.netcrop_rdpg}
\alias{summary.netcrop_rdpg}
\alias{print.summary.netcrop_rdpg}
\alias{plot.netcrop_rdpg}
\title{Select RDPG dimension with NETCROP}
\usage{
netcrop_rdpg(
  A,
  d_candidates,
  num_subnetworks = NULL,
  overlap_size = NULL,
  nrep = 1L,
  losses = "sse",
  eig_options = list(),
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  parameter_select_options = list(),
  retain_intermediates = c("all", "minimal")
)

\method{print}{netcrop_rdpg}(x, ...)

\method{summary}{netcrop_rdpg}(object, ...)

\method{print}{summary.netcrop_rdpg}(x, ...)

\method{plot}{netcrop_rdpg}(x, aggregate = TRUE, ...)
}
\arguments{
\item{A}{Finite square symmetric numeric adjacency matrix with zero diagonal,
including a supported sparse \code{Matrix} object.}

\item{d_candidates}{Non-empty vector of positive integer latent dimensions;
duplicates are discarded. Dimensions must be smaller than the effective
subgraph size; conventionally the candidate vector is \code{1:5}.}

\item{num_subnetworks}{Optional integer number of subnetworks, at least 2. If
it or \code{overlap_size} is \code{NULL}, missing values are selected with
\code{\link[=netcrop_param_select]{netcrop_param_select()}}.}

\item{overlap_size}{Optional positive integer overlap size. It must leave
enough non-overlap nodes for the requested subnetworks and dimensions.}

\item{nrep}{Positive integer number of independent NETCROP repetitions.}

\item{losses}{Character vector naming prediction-loss functions available in
the calling environment, such as \code{"sse"}.}

\item{eig_options}{Named list of additional options passed to
\code{\link[=eig_decomp]{eig_decomp()}}.}

\item{ncores}{Positive integer worker count used across decomposition,
embedding, alignment, and loss tasks.}

\item{seed}{Optional nonnegative integer-like reproducibility seed.}

\item{verbose}{Whether to print progress messages.}

\item{force_windows}{Whether to force the Windows-compatible parallel
backend, including for backend testing on other platforms.}

\item{ram_check}{Whether to run RAM preflight checks before major operations.}

\item{parameter_select_options}{Named list of additional options passed to
\code{\link[=netcrop_param_select]{netcrop_param_select()}} when partition parameters are selected
automatically.}

\item{retain_intermediates}{Either \code{"all"} or \code{"minimal"}, controlling how
many embeddings and intermediate arrays are retained.}

\item{x}{A \code{netcrop_rdpg} or \code{summary.netcrop_rdpg} object. For plotting, it
is the first result to display or compare.}

\item{...}{Additional plotting arguments or further \code{netcrop_rdpg} objects
for comparison; ignored by print and summary methods.}

\item{object}{A fitted \code{netcrop_rdpg} object to summarize.}

\item{aggregate}{Whether a plot should aggregate loss curves across
repetitions. A \code{netcrop_rdpg} object in this position instead starts a
comparison plot.}
}
\value{
\code{netcrop_rdpg()} returns an object of class \code{netcrop_rdpg} containing
candidate-wise cross-validation losses, repetition and overall dimension
selections, split and eigenvalue diagnostics, retained intermediates,
resource information, and timing. \code{summary.netcrop_rdpg()} returns a
compact summary object; print methods return their input invisibly, and the
plot method returns a \code{ggplot} object.
}
\description{
Selects a symmetric random-dot-product-graph (RDPG) dimension by
overlapping-subnetwork cross-validation. The procedure decomposes every
subnetwork, constructs each candidate embedding, aligns non-reference
embeddings through the overlap, and evaluates every unordered pair of
non-overlap pieces.
}
\examples{
\donttest{
A <- generate_rdpg(n = 200, d = 3, seed = 7, ncores = 1)
fit <- netcrop_rdpg(
  A, d_candidates = 1:5, num_subnetworks = 2, overlap_size = 30,
  ncores = 1, seed = 8, verbose = FALSE
)
fit
summary(fit)
}
}
\seealso{
\code{\link[=ecv_stability_rdpg]{ecv_stability_rdpg()}}, \code{\link[=ase]{ase()}}
}
