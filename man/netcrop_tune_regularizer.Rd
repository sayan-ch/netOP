% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x5_netcrop_regularizer.R
\name{netcrop_tune_regularizer}
\alias{netcrop_tune_regularizer}
\alias{print.netcrop_regularizer}
\alias{summary.netcrop_regularizer}
\alias{print.summary.netcrop_regularizer}
\alias{plot.netcrop_regularizer}
\title{Tune a network regularizer with NETCROP}
\usage{
netcrop_tune_regularizer(
  A,
  K,
  tau_candidates,
  use_dcbm = FALSE,
  num_subnetworks = NULL,
  overlap_size = NULL,
  nrep = 1L,
  use_laplacian = FALSE,
  dcbm_est_method = c("plugin", "spectral"),
  losses = "sse",
  loss_types = NULL,
  label_reference = c("full_network", "leave_pair_out"),
  spectral_options = list(),
  cluster_options = list(),
  estimator_options = list(),
  matching_method = c("greedy", "hungarian", "brute_force"),
  confirm_large = NULL,
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  parameter_select_options = list(),
  retain_intermediates = c("all", "minimal")
)

\method{print}{netcrop_regularizer}(x, ...)

\method{summary}{netcrop_regularizer}(object, ...)

\method{print}{summary.netcrop_regularizer}(x, ...)

\method{plot}{netcrop_regularizer}(x, aggregate = TRUE, ...)
}
\arguments{
\item{A}{Finite symmetric, loop-free adjacency matrix.}

\item{K}{Fixed positive number of communities.}

\item{tau_candidates}{Unique finite nonnegative regularization candidates.}

\item{use_dcbm}{Use a degree-corrected rather than ordinary block model.}

\item{num_subnetworks}{Number of overlapping subnetworks; \code{NULL} selects it
automatically.}

\item{overlap_size}{Number of overlap nodes; \code{NULL} selects it automatically.}

\item{nrep}{Positive number of independently sampled partitions.}

\item{use_laplacian}{Cluster a regularized graph Laplacian.}

\item{dcbm_est_method}{DCBM estimator, either \code{"plugin"} or \code{"spectral"}.}

\item{losses}{Canonical validation losses to evaluate.}

\item{loss_types}{Optional named mapping for compatible legacy loss labels.}

\item{label_reference}{Whether label losses use a full-network reference or
a leave-pair-out reference.}

\item{spectral_options}{Named list of additional options passed to
\code{\link[=spectral_cluster]{spectral_cluster()}}.}

\item{cluster_options}{Named list of additional options passed to
\code{\link[stats:kmeans]{stats::kmeans()}} for SBM fitting or \code{\link[cluster:clara]{cluster::clara()}} for DCBM fitting.}

\item{estimator_options}{Named list of additional options passed to
\code{\link[=estimate_sbm]{estimate_sbm()}} for SBM fitting or \code{\link[=estimate_dcbm]{estimate_dcbm()}} for DCBM fitting.}

\item{matching_method}{Label-alignment method.}

\item{confirm_large}{Confirm potentially factorial brute-force matching.}

\item{ncores}{Positive worker count.}

\item{seed}{Optional nonnegative reproducibility seed.}

\item{verbose}{Print progress messages.}

\item{force_windows}{Use the Windows-compatible parallel backend.}

\item{ram_check}{Report conservative RAM demand.}

\item{parameter_select_options}{Named list of additional options passed to
\code{\link[=netcrop_param_select]{netcrop_param_select()}} for automatic partition selection.}

\item{retain_intermediates}{Retain all or minimal intermediate results.}

\item{x, object}{A fitted \code{netcrop_regularizer} object handled by an S3
method.}

\item{...}{Additional arguments for S3 method compatibility.}

\item{aggregate}{Aggregate repetitions when plotting.}
}
\value{
A \code{netcrop_regularizer} object containing candidate loss curves,
per-repetition and overall selections, resolved options, partition and
matching diagnostics, worker counts, timings, and optional intermediates.
}
\description{
Selects a spectral regularization parameter by overlapping-subnetwork
cross-validation for an SBM or DCBM.
}
\details{
The function clusters overlapping subnetworks for every candidate, aligns
their labels, estimates block-model probabilities, and evaluates held-out
edges between non-overlap pieces. Missing partition parameters are selected
with \code{\link[=netcrop_param_select]{netcrop_param_select()}}. \code{K = 1} returns invisibly because no
clustering-based regularizer selection is required.
}
\examples{
\donttest{
A <- generate_sbm(n = 200, K = 3, alpha = 0.5, beta = 0.1,
                  seed = 31, ncores = 1)
fit <- netcrop_tune_regularizer(
  A, K = 3, tau_candidates = c(0, 0.1),
  num_subnetworks = 2, overlap_size = 20,
  nrep = 1, ncores = 1, seed = 32, verbose = FALSE
)
fit
}
}
\seealso{
\code{\link[=dkest_tune_regularizer]{dkest_tune_regularizer()}}, \code{\link[=mult_reg_spectral_cluster]{mult_reg_spectral_cluster()}}
}
