% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x6_other_algos.R
\name{oracle_plotter}
\alias{oracle_plotter}
\title{Compare clustering methods with known labels}
\usage{
oracle_plotter(
  A,
  g_true,
  tau_candidates,
  K = NULL,
  netcrop_outcomes = NULL,
  dkest_outcomes = NULL,
  include_netcrop_mean = TRUE,
  include_netcrop_mode = TRUE,
  losses = NULL,
  engines = c("sonnet", "spectral_cluster"),
  matching_method = c("greedy", "hungarian", "brute_force"),
  confirm_large = NULL,
  sonnet_options = list(),
  spectral_cluster_options = list(),
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE
)
}
\arguments{
\item{A}{One adjacency matrix or a list matching \code{g_true}.}

\item{g_true}{Ground-truth labels or a list matching \code{A}.}

\item{tau_candidates}{Unique finite nonnegative regularization candidates.}

\item{K}{Optional fixed number of communities; inferred from \code{g_true} when
omitted.}

\item{netcrop_outcomes, dkest_outcomes}{Optional matching tuner results.}

\item{include_netcrop_mean, include_netcrop_mode}{Include NETCROP repetition
summaries in addition to its first repetition.}

\item{losses}{Optional NETCROP loss names to include.}

\item{engines}{Fit \code{"sonnet"}, \code{"spectral_cluster"}, or both.}

\item{matching_method}{Label-alignment method used for accuracy.}

\item{confirm_large}{Confirm potentially factorial brute-force matching.}

\item{sonnet_options, spectral_cluster_options}{Named fitter option lists.}

\item{ncores}{Positive worker count.}

\item{seed}{Optional nonnegative reproducibility seed.}

\item{verbose}{Print progress messages.}

\item{force_windows}{Use the Windows-compatible parallel backend.}

\item{ram_check}{Report conservative RAM demand.}
}
\value{
Invisibly returns the rendered \code{ggplot}. Raw accuracy data,
aggregated plotting data, effective fit metadata, and diagnostics are
attached as attributes.
}
\description{
Evaluates SONNET, spectral clustering, or both against known community
labels over candidate regularizers.
}
\details{
Optional NETCROP and DKEST results add their selected regularizers to the
comparison, including off-grid values. Accuracy is one minus the validated
label-matching mismatch rate; multiple networks are summarized with means
and standard deviations.
}
\examples{
\donttest{
A <- generate_sbm(n = 200, K = 3, alpha = 0.5, beta = 0.1,
                  seed = 41, ncores = 1)
truth <- get_generator_parameters(A)$g_true
oracle_plotter(
  A, g_true = truth, tau_candidates = c(0, 0.1), K = 3,
  engines = "spectral_cluster", ncores = 1, seed = 42,
  verbose = FALSE,
  spectral_cluster_options = list(spectral_engine = "base")
)
}
}
\seealso{
\code{\link[=spectral_cluster]{spectral_cluster()}}, \code{\link[=sonnet]{sonnet()}}
}
