% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_basic_helpers.R
\name{run_simulations}
\alias{run_simulations}
\title{Run reproducible simulation jobs}
\usage{
run_simulations(
  one_simulation,
  nsim,
  ...,
  use_parallel_simulations = FALSE,
  ncores_outer = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  seeds = NULL,
  results_file = NULL,
  action = c("replace", "resume", "archive"),
  retry_failed = TRUE,
  continue_on_error = TRUE,
  measure_resources = FALSE,
  show_progress = TRUE,
  force_windows = FALSE
)
}
\arguments{
\item{one_simulation}{Function implementing one simulation. It must accept
a named \code{simulation} argument containing the one-based simulation number.}

\item{nsim}{Positive number of simulations.}

\item{...}{Additional arguments forwarded to \code{one_simulation}.}

\item{use_parallel_simulations}{Parallelize the outer simulation loop. The
default avoids accidental nested parallelism.}

\item{ncores_outer}{Positive number of outer-loop workers.}

\item{seed}{Optional nonnegative base seed. Simulation \code{i} uses
\code{seed + i - 1}, reduced to R's supported integer range.}

\item{seeds}{Optional vector of \code{nsim} explicit nonnegative seeds. Supply
either \code{seed} or \code{seeds}, not both.}

\item{results_file}{Optional trusted RDS path used for durable results.}

\item{action}{How to handle an existing results file: \code{"replace"},
\code{"resume"}, or \code{"archive"}.}

\item{retry_failed}{When resuming, rerun records whose success field is
\code{FALSE}.}

\item{continue_on_error}{Return all records when simulations fail. If
\code{FALSE}, save available records and stop with a summary error.}

\item{measure_resources}{Measure elapsed time and peak additional R-managed
RAM for each simulation.}

\item{show_progress}{Print a compact status line for each newly run
simulation when supported by the backend.}

\item{force_windows}{Use the Windows-compatible future backend.}
}
\value{
A list of \code{nsim} simulation records, invisibly when a resumed run
has no work remaining.
}
\description{
Executes a simulation function repeatedly with optional outer-loop
parallelism, reproducible per-simulation seeds, durable RDS results,
resuming, and resource measurements. \code{one_simulation} must accept a named
\code{simulation} argument. Values supplied through \code{...} are forwarded
unchanged.
}
\details{
When \code{results_file} is supplied, each simulation is stored as a record with
its number, seed, success status, elapsed time, optional peak RAM, returned
value, and error message. Existing results can be replaced, resumed, or
archived.
}
\examples{
run_simulations(
  one_simulation = function(simulation, offset) simulation + offset,
  nsim = 2, offset = 10, seed = 1, show_progress = FALSE
)
}
\seealso{
\code{\link[=uni_mclapply]{uni_mclapply()}}, \code{\link[=get_generator_parameters]{get_generator_parameters()}}
}
