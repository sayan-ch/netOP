% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_math_helpers.R
\name{singular_decomp}
\alias{singular_decomp}
\title{Compute a singular-value decomposition}
\usage{
singular_decomp(
  A,
  d,
  only_values = FALSE,
  nu = if (only_values) 0L else d,
  nv = if (only_values) 0L else d,
  scale_by = c("none", "dimension", "sparsity"),
  use_laplacian = FALSE,
  engine = c("rspectra", "base", "irlba"),
  force_engine = FALSE,
  order_by = c("value", "magnitude"),
  safe_d_multiplier = 1,
  validate_inputs = TRUE
)
}
\arguments{
\item{A}{A finite dense or sparse matrix.}

\item{d}{Positive number of singular components to return.}

\item{only_values}{Return singular values without singular vectors.}

\item{nu, nv}{Numbers of left and right singular vectors to return.}

\item{scale_by}{Scale by \code{"none"}, matrix \code{"dimension"}, or entry
\code{"sparsity"}.}

\item{use_laplacian}{Transform \code{A} with \code{\link[=graph_laplacian]{graph_laplacian()}} first.}

\item{engine}{Decomposition backend: \code{"rspectra"}, \code{"base"}, or \code{"irlba"}.}

\item{force_engine}{Stop instead of falling back when the selected partial
backend is unavailable or fails.}

\item{order_by}{Component ordering convention; singular values make
\code{"value"} and \code{"magnitude"} equivalent.}

\item{safe_d_multiplier}{Multiplier for extra partial components computed
before selecting the requested components.}

\item{validate_inputs}{Validate matrix and option inputs.}
}
\value{
A list with \code{values} and requested \code{u} and \code{v} matrices.
}
\description{
Computes selected singular values and optional left and right vectors.
Partial backends fall back to \code{\link[base:svd]{base::svd()}} unless \code{force_engine = TRUE}.
}
\examples{
singular_decomp(diag(c(3, 2, 1)), d = 2, engine = "base")
}
\seealso{
\code{\link[=eig_decomp]{eig_decomp()}}, \code{\link[=truncated_svd_reconstruct]{truncated_svd_reconstruct()}}
}
