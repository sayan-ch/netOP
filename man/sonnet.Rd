% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x1_sonnet.R
\name{sonnet}
\alias{sonnet}
\alias{print.sonnet}
\alias{summary.sonnet}
\alias{print.summary.sonnet}
\title{Fit a SONNET network clustering}
\usage{
sonnet(
  A,
  K,
  num_subnetworks = NULL,
  overlap_size = NULL,
  extra_nrep = 0L,
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  seed = NULL,
  matching_method = c("greedy", "hungarian", "brute_force"),
  confirm_large = TRUE,
  verbose = TRUE,
  force_windows = FALSE,
  ram_check = FALSE,
  share_overlap = FALSE,
  parameter_select_options = list(),
  laplacian = FALSE,
  regularize_tau = 0,
  regularize_subnetworks = TRUE,
  ...
)

\method{print}{sonnet}(x, ...)

\method{summary}{sonnet}(object, ...)

\method{print}{summary.sonnet}(x, ...)
}
\arguments{
\item{A}{Non-empty finite square numeric adjacency matrix, including a
supported sparse \code{Matrix} object.}

\item{K}{Positive integer number of communities, no larger than \code{nrow(A)}.}

\item{num_subnetworks}{Optional positive integer number of subnetworks. If it
or \code{overlap_size} is \code{NULL}, missing partition values are selected with
\code{\link[=sonnet_param_select]{sonnet_param_select()}}.}

\item{overlap_size}{Optional positive integer number of overlap nodes, no
larger than \code{nrow(A)}.}

\item{extra_nrep}{Nonnegative integer number of additional partition and
clustering repetitions beyond the initial run.}

\item{ncores}{Positive integer worker count used across fitting and label
alignment tasks.}

\item{seed}{Optional nonnegative integer-like reproducibility seed.}

\item{matching_method}{Label-alignment method: \code{"greedy"}, \code{"hungarian"}, or
\code{"brute_force"}.}

\item{confirm_large}{Whether to require confirmation before an unusually
large brute-force label-matching problem.}

\item{verbose}{Whether to print progress and timing messages.}

\item{force_windows}{Whether to force the Windows-compatible parallel
backend, including for backend testing on other platforms.}

\item{ram_check}{Whether to run RAM preflight checks before major operations.}

\item{share_overlap}{Whether every repetition uses the same overlap nodes.
If \code{FALSE}, each repetition samples its overlap independently.}

\item{parameter_select_options}{Named list of additional options passed to
\code{\link[=sonnet_param_select]{sonnet_param_select()}} when partition parameters are selected
automatically.}

\item{laplacian}{Whether to convert adjacency matrices to graph Laplacians
before spectral clustering.}

\item{regularize_tau}{Nonnegative Laplacian regularization strength.}

\item{regularize_subnetworks}{Whether Laplacian conversion and regularization
are performed separately within each subnetwork. If \code{FALSE}, the full
network is transformed before subnetworks are extracted.}

\item{...}{Additional named arguments passed to \code{\link[=spectral_cluster]{spectral_cluster()}}. \code{A},
\code{U}, and \code{K} are reserved and cannot be supplied here. For the print and
summary methods, additional arguments are currently ignored.}

\item{x}{A fitted \code{sonnet} or \code{summary.sonnet} object to print.}

\item{object}{A fitted \code{sonnet} object to summarize.}
}
\value{
\code{sonnet()} returns a fitted object of class \code{sonnet} containing final
labels, aligned labels and memberships, subnetwork fits and node sets,
parameters, core allocation, and timing. \code{summary.sonnet()} returns a
\code{summary.sonnet} object. Print methods return their input invisibly.
}
\description{
Fits scalable network clustering by partitioning a dense or sparse network
into overlapping subnetworks, clustering each subnetwork, aligning labels
through their overlaps, and aggregating repetition-level memberships. The
overlap may be shared across repetitions or sampled independently.
}
\examples{
\donttest{
A <- generate_sbm(n = 200, K = 3, alpha = 0.5, beta = 0.1,
                  seed = 3, ncores = 1)
fit <- sonnet(
  A, K = 3, num_subnetworks = 2, overlap_size = 30,
  ncores = 1, seed = 4, verbose = FALSE, spectral_engine = "base"
)
fit
summary(fit)
}
}
\seealso{
\code{\link[=sonnet_shared_overlap]{sonnet_shared_overlap()}}, \code{\link[=sonnet_independent_overlap]{sonnet_independent_overlap()}},
\code{\link[=spectral_cluster]{spectral_cluster()}}
}
