% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x1_sonnet.R
\name{sonnet_shared_overlap}
\alias{sonnet_shared_overlap}
\alias{sonnet_independent_overlap}
\title{Fit SONNET overlap variants}
\usage{
sonnet_shared_overlap(...)

sonnet_independent_overlap(...)
}
\arguments{
\item{...}{Named arguments passed to the SONNET fitting engine: \code{A}, \code{K},
\code{num_subnetworks}, \code{overlap_size}, \code{extra_nrep}, \code{ncores}, \code{seed},
\code{matching_method}, \code{confirm_large}, \code{verbose}, \code{force_windows}, \code{ram_check},
\code{parameter_select_options}, \code{laplacian}, \code{regularize_tau}, and
\code{regularize_subnetworks}, plus supported \code{\link[=spectral_cluster]{spectral_cluster()}} arguments.
Do not supply \code{share_overlap}; each wrapper sets it.}
}
\value{
A fitted object of class \code{sonnet}, containing final labels,
repetition-level labels and memberships, split information, parameters,
core allocation, timing, and the matched call.
}
\description{
Fit SONNET using either one overlap set shared by every repetition or an
independently sampled overlap set in each repetition. Both wrappers run the
partition, spectral-clustering, label-alignment, and modal-aggregation
workflow used by \code{\link[=sonnet]{sonnet()}}.
}
\examples{
\donttest{
A <- generate_sbm(n = 200, K = 3, alpha = 0.5, beta = 0.1,
                  seed = 1, ncores = 1)
shared <- sonnet_shared_overlap(
  A = A, K = 3, num_subnetworks = 2, overlap_size = 30,
  extra_nrep = 1, ncores = 1, seed = 2, verbose = FALSE,
  spectral_engine = "base"
)
independent <- sonnet_independent_overlap(
  A = A, K = 3, num_subnetworks = 2, overlap_size = 30,
  extra_nrep = 1, ncores = 1, seed = 2, verbose = FALSE,
  spectral_engine = "base"
)
}
}
\seealso{
\code{\link[=sonnet]{sonnet()}}, \code{\link[=sonnet_param_select]{sonnet_param_select()}}
}
