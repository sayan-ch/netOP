% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x0_helpers.R
\name{sonnet_param_select}
\alias{sonnet_param_select}
\title{Select SONNET partition parameters}
\usage{
sonnet_param_select(
  n,
  K = 5L,
  theta = 3,
  q = 0.005,
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  s_lower = 2,
  s_upper = 50,
  o_lower = NULL,
  o_upper = NULL
)
}
\arguments{
\item{n}{Positive integer network size, at least 3.}

\item{K}{Positive integer number of communities, no larger than \code{n}; used to
choose the default lower overlap bound.}

\item{theta}{Positive computational-complexity exponent.}

\item{q}{Number in \verb{(0, 1]} giving the target sequential computational
fraction per available core.}

\item{ncores}{Positive integer number of cores included in the computational
budget.}

\item{s_lower, s_upper}{Numeric lower and upper bounds for the number of
subnetworks. Bounds are normalized to admissible integers.}

\item{o_lower, o_upper}{Optional numeric lower and upper bounds for overlap
size. \code{NULL} uses \code{min(K^3, n - s_lower)} for the lower bound and the
largest admissible overlap for the upper bound.}
}
\value{
A named list containing the selected \code{num_subnetworks} and
\code{overlap_size}, piece size, objective and computational fractions,
requested and effective tuning values, search counts, and normalized
bounds.
}
\description{
Selects the number of subnetworks and their common overlap by a constrained
integer grid search. Computational cost is evaluated as
\code{s * ((o + (n - o) / s) / n)^theta} without forming \code{n^theta}. Feasible
candidates have cost no greater than \code{q * ncores}, and \code{(n - o)} must be
divisible by \code{s}. If none meets the requested budget, the least-cost
admissible candidate is returned and the effective \code{q} is increased.
}
\examples{
sonnet_param_select(n = 200, K = 3, ncores = 1)
}
\seealso{
\code{\link[=sonnet]{sonnet()}}, \code{\link[=netcrop_param_select]{netcrop_param_select()}}
}
