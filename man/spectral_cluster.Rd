% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_embedders.R
\name{spectral_cluster}
\alias{spectral_cluster}
\title{Cluster a network spectrally}
\usage{
spectral_cluster(
  A = NULL,
  U = NULL,
  K,
  laplacian = FALSE,
  normalize_laplacian = TRUE,
  regularize_tau = 0,
  handle_zero_degree_nodes = c("none", "random_label", "remove"),
  row_normalize = FALSE,
  spectral_method = c("eigen", "svd"),
  spectral_engine = c("RSpectra", "irlba", "base"),
  spectral_options = list(),
  cluster_engine = c("clara", "kmeans", "pam"),
  cluster_options = list(),
  ram_check = FALSE,
  validate_inputs = TRUE
)
}
\arguments{
\item{A}{Optional finite square dense or sparse adjacency matrix. Supply
either \code{A} or \code{U}.}

\item{U}{Optional finite matrix whose rows are clustered directly. Supply
either \code{U} or \code{A}.}

\item{K}{Positive number of communities.}

\item{laplacian}{Whether to cluster a graph-Laplacian representation.}

\item{normalize_laplacian}{Whether a requested Laplacian is normalized.}

\item{regularize_tau}{Nonnegative adjacency regularization parameter.}

\item{handle_zero_degree_nodes}{Policy for zero-degree nodes: leave them in
the fit, assign random labels, or remove them during clustering.}

\item{row_normalize}{Whether to normalize spectral-representation rows.}

\item{spectral_method}{Whether to use an eigendecomposition or SVD.}

\item{spectral_engine}{Spectral backend: \code{"RSpectra"}, \code{"irlba"}, or base R.}

\item{spectral_options}{Named list of additional spectral-backend options.}

\item{cluster_engine}{Clustering backend: \code{"clara"}, \code{"kmeans"}, or \code{"pam"}.}

\item{cluster_options}{Named list of additional clustering-backend options.}

\item{ram_check}{Whether to report a conservative RAM preflight estimate.}

\item{validate_inputs}{Whether to validate inputs; only audited internal
callers should disable this.}
}
\value{
A fitted spectral-clustering object with labels and diagnostics.
}
\description{
Clusters a dense or sparse network into \code{K} communities using a configurable
spectral representation and clustering backend.
}
\details{
If \code{U} is supplied, it is clustered directly and \code{A}, including its
degree information, is ignored. Otherwise asymmetric \code{A} is converted to
\code{A + t(A)}. Regularization uses
\eqn{A_\tau = A + \tau \operatorname{mean}(degree) / n} before direct
decomposition or Laplacian construction.
}
\examples{
A <- generate_sbm(n = 200, K = 3, alpha = 0.4, beta = 0.1,
                  seed = 9, ncores = 1)
fit <- spectral_cluster(A, K = 3, spectral_engine = "base",
                        cluster_engine = "kmeans")
table(fit$g_hat)
}
\seealso{
\code{\link[=ase]{ase()}}, \code{\link[=estimate_sbm]{estimate_sbm()}}, \code{\link[=sonnet]{sonnet()}}
}
