% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_basic_helpers.R
\name{uni_mclapply}
\alias{uni_mclapply}
\title{Apply a function consistently across platforms}
\usage{
uni_mclapply(
  X,
  FUN,
  ...,
  ncores = max(floor(parallel::detectCores()/2), 1L, na.rm = TRUE),
  force_windows = FALSE,
  stop_on_error = FALSE,
  manage_future_plan = TRUE,
  future_packages = NULL,
  suppress_worker_renv_sync_check = TRUE
)
}
\arguments{
\item{X}{A vector, list, or other collection accepted by lapply-style
functions.}

\item{FUN}{Function applied to each element of \code{X}.}

\item{...}{Additional arguments forwarded to \code{FUN}.}

\item{ncores}{Positive number of workers. The default is half the detected
logical cores, rounded down, with a minimum of one.}

\item{force_windows}{Use the Windows-compatible future backend even on
another operating system.}

\item{stop_on_error}{Stop with failed task indices and worker messages
instead of falling back or returning worker-level errors.}

\item{manage_future_plan}{Create and restore a temporary multisession plan.
Set \code{FALSE} only when the caller manages the future plan.}

\item{future_packages}{Optional package names loaded by future workers,
including packages needed for S4 method registration.}

\item{suppress_worker_renv_sync_check}{Temporarily suppress repeated renv
synchronization messages while multisession workers start.}
}
\value{
A list containing one result per element of \code{X}.
}
\description{
Applies \code{FUN} to each element of \code{X}. Unix-like systems first attempt
\code{\link[parallel:mclapply]{parallel::mclapply()}}. Windows, or \code{force_windows = TRUE}, uses
\code{future.apply::future_lapply()}. A temporary multisession plan is created
and restored by default. Backend failures fall back to \code{\link[base:lapply]{base::lapply()}}
unless \code{stop_on_error = TRUE}.
}
\examples{
uni_mclapply(1:3, function(x) x^2, ncores = 1)
}
\seealso{
\code{\link[=run_simulations]{run_simulations()}}, \code{\link[=sonnet]{sonnet()}}
}
